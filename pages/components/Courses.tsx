import React from 'react';
import styles from './courses.module.scss';

const Courses = () => {
  return (
    <div className={styles.courses}>
      <div className={styles.courseItems}>courseItems</div>
      <div>bottom</div>
      courses
    </div>
  );
};

export default Courses;
